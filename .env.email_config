# Email Configuration for Local Development
# Since SMTP ports are blocked locally, suppress email sending
# Emails will work fine in production where ports are open

# Suppress email sending in local development (prevents timeout errors)
MAIL_SUPPRESS_SEND=True

# Production email settings (will be used when MAIL_SUPPRESS_SEND=False)
# MAIL_SERVER=mail.niko-free.com
# MAIL_PORT=465
# MAIL_USE_TLS=False
# MAIL_USE_SSL=True
# MAIL_USERNAME=support@niko-free.com
# MAIL_PASSWORD=your-password-here
